# setup:
#   - command:  gcloud storage buckets create {{ identifier }} 
#     teardown_command: gcloud storage buckets delete  {{ identifier }}
#     flags: 
#       uniform-bucket-level-access: ""
#       location: asia-southeast1
storage_bucket_uniform_bucket_level_access:
  steps:
  - identifier: gs://{{ prefix }}-bucket
    command: gcloud storage buckets create {{ identifier }} --uniform-bucket-level-access --location=asia-southeast1
    expected_return_code: 0
    teardown_command: gcloud storage buckets delete  {{ identifier }}
  markers:
    - storage