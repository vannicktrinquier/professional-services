[pytest]
log_cli = 1
log_cli_level = DEBUG
render_collapsed = passed, failed, error
addopts = --html=report.html --self-contained-html
markers = 
    cloudkms
    cloudsql
    dns
    dns-policy
    dns-managed-zones
    iam
    mysql
    postgresql
    sqlserver
    storage
    gke

