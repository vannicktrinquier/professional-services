#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictInsecureProtocolsRule")

#@ def build_condition(allowed_source_ranges):
#@   #! Insecure legacy protocols that should not be exposed to the Internet
#@   #! Telnet: TCP:23 (unencrypted remote access)
#@   #! FTP: TCP:21 (unencrypted file transfer)
#@   #! HTTP: TCP:80 (unencrypted web traffic)
#@   tcp_ports_list = ['23', '21', '80']
#@   sanitized_allowed_ranges = [rng for rng in allowed_source_ranges if rng] or []
#@   source_check = '!resource.sourceRanges.all(range, range in ' + str(sanitized_allowed_ranges) + ')'
#@   tcp_check = "rule.IPProtocol == 'tcp' && rule.ports.exists(port, port in " + str(tcp_ports_list) + ")"
#@   return "resource.direction.matches('INGRESS') && " + source_check + " && resource.allowed.exists(rule, " + tcp_check + ")"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/Firewall
condition: #@ build_condition(constraint.params().allowed_source_ranges)
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict VPC Firewall rules allowing insecure protocol access from the Internet
description: Ensure that insecure legacy protocols (Telnet, FTP, HTTP) are not accessible from the Internet when using VPC Firewall Rule
#@ end
