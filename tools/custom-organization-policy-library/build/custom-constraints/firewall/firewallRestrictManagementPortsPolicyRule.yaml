#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictManagementPortsPolicyRule")

#@ def build_condition(allowed_source_ranges):
#@   #! Administrative and management interfaces
#@   #! Cisco Secure WebSM: TCP:9090
#@   sanitized_allowed_ranges = [rng for rng in allowed_source_ranges if rng] or []
#@   source_check = '!rule.match.srcIpRanges.all(ipRange, ipRange in ' + str(sanitized_allowed_ranges) + ')'
#@   tcp_ports = "port == '9090'"
#@   return "resource.rules.exists(rule, rule.priority < 2147483644 && rule.direction == 'INGRESS' && " + source_check + " && rule.match.layer4Configs.exists(l4config, l4config.ipProtocol == 'tcp' && l4config.ports.exists(port, " + tcp_ports + ")))"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/FirewallPolicy
condition: #@ build_condition(constraint.params().allowed_source_ranges)
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict Firewall Policy rules allowing management port access from the Internet
description: Ensure that management interfaces (Cisco Secure WebSM) are not accessible from the Internet when using Firewall Policy Rule
#@ end
