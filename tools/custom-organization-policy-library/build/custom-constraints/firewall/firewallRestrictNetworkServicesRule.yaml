#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictNetworkServicesRule")

#@ def build_condition(allowed_source_ranges):
#@   #! Core network infrastructure protocols
#@   #! DNS: TCP/UDP:53
#@   #! NetBIOS: TCP/UDP:137-139
#@   #! Deny unless every source range is explicitly allow-listed
#@   sanitized_allowed_ranges = [rng for rng in allowed_source_ranges if rng] or []
#@
#@   lines = [
#@     "resource.direction.matches('INGRESS') &&",
#@     "  !resource.sourceRanges.all(range,",
#@     "      range in " + str(sanitized_allowed_ranges),
#@     "  ) &&",
#@     "  resource.allowed.exists(rule,",
#@     "    rule.ports.exists(port,",
#@     "        rule.IPProtocol == 'tcp' && (",
#@     "            port == '53' ||",
#@     "            port == '137' ||",
#@     "            port == '138' ||",
#@     "            port == '139'",
#@     "        ) ||",
#@     "        rule.IPProtocol == 'udp' && (",
#@     "            port == '53' ||",
#@     "            port == '137' ||",
#@     "            port == '138' ||",
#@     "            port == '139'",
#@     "        )",
#@     "    )",
#@     "  )"
#@   ]
#@   return "\n".join(lines)
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/Firewall
#@ condition_text = build_condition(constraint.params().allowed_source_ranges)
condition: #@ condition_text
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict VPC Firewall rules allowing network service access from the Internet
description: Ensure that network infrastructure services (DNS, NetBIOS) are not accessible from the Internet when using VPC Firewall Rule
#@ end
