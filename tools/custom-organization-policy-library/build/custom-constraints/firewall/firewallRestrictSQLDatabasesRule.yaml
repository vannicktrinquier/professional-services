#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictSQLDatabasesRule")

#@ def build_condition(allowed_source_ranges):
#@   #! SQL Relational Databases
#@   #! MySQL: TCP 3306
#@   #! Oracle: TCP 1521, 2483-2484; UDP 2483-2484
#@   #! PostgreSQL: TCP/UDP 5432
#@   #! Deny unless every source range is explicitly allow-listed
#@   sanitized_allowed_ranges = [rng for rng in allowed_source_ranges if rng] or []
#@   source_check = "!resource.sourceRanges.all(range, range in " + str(sanitized_allowed_ranges) + ")"
#@   #! SINGLE ports.exists() with protocol checks inside - remove extra parens to avoid depth
#@   tcp_port_check = " || ".join(["port == '" + p + "'" for p in ['3306', '1521', '2483', '2484', '5432']])
#@   udp_port_check = " || ".join(["port == '" + p + "'" for p in ['2483', '2484', '5432']])
#@   allowed_check = "rule.ports.exists(port, rule.IPProtocol == 'tcp' && " + tcp_port_check + " || rule.IPProtocol == 'udp' && " + udp_port_check + ")"
#@   return "resource.direction.matches('INGRESS') && " + source_check + " && resource.allowed.exists(rule, " + allowed_check + ")"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/Firewall
condition: #@ build_condition(constraint.params().allowed_source_ranges)
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict VPC Firewall rules allowing SQL database port access from the Internet
description: Ensure that SQL database ports (MySQL, Oracle, PostgreSQL) are not accessible from the Internet when using VPC Firewall Rule
#@ end
