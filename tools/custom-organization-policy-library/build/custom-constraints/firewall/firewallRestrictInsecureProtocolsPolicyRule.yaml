#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictInsecureProtocolsPolicyRule")

#@ def build_condition(allowed_source_ranges):
#@   #! Insecure legacy protocols that should not be exposed to the Internet
#@   #! Telnet: TCP:23 (unencrypted remote access)
#@   #! FTP: TCP:21 (unencrypted file transfer)
#@   #! HTTP: TCP:80 (unencrypted web traffic)
#@   tcp_ports_list = ['23', '21', '80']
#@   source_check = '!rule.match.srcIpRanges.all(ipRange, ipRange in ' + str(allowed_source_ranges) + ')'
#@   tcp_ports = "port in " + str(tcp_ports_list)
#@   return "resource.rules.exists(rule, rule.priority < 2147483644 && rule.direction == 'INGRESS' && " + source_check + " && rule.match.layer4Configs.exists(l4config, l4config.ipProtocol == 'tcp' && l4config.ports.exists(port, " + tcp_ports + ")))"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/FirewallPolicy
condition: #@ build_condition(constraint.params().allowed_source_ranges)
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict Firewall Policy rules allowing insecure protocol access from the Internet
description: Ensure that insecure legacy protocols (Telnet, FTP, HTTP) are not accessible from the Internet when using Firewall Policy Rule
#@ end
