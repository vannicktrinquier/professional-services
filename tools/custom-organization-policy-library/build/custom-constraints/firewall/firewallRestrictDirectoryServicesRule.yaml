#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictDirectoryServicesRule")

#@ def build_condition(allowed_source_ranges):
#@   #! Directory and authentication services
#@   #! SMB/CIFS: TCP/UDP:445
#@   #! LDAP: TCP:389, 636; UDP:389
#@   tcp_ports_list = ['445', '389', '636']
#@   udp_ports_list = ['445', '389']
#@   source_check = '!resource.sourceRanges.all(range, range in ' + str(allowed_source_ranges) + ')'
#@   tcp_check = "rule.IPProtocol == 'tcp' && rule.ports.exists(port, port in " + str(tcp_ports_list) + ")"
#@   udp_check = "rule.IPProtocol == 'udp' && rule.ports.exists(port, port in " + str(udp_ports_list) + ")"
#@   return "resource.direction.matches('INGRESS') && " + source_check + " && resource.allowed.exists(rule, " + tcp_check + " || " + udp_check + ")"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/Firewall
condition: #@ build_condition(constraint.params().allowed_source_ranges)
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict VPC Firewall rules allowing directory service access from the Internet
description: Ensure that directory and authentication services (SMB/CIFS, LDAP) are not accessible from the Internet when using VPC Firewall Rule
#@ end
