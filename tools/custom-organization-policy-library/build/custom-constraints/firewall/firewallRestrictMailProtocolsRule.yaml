#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictMailProtocolsRule")

#@ def build_condition(allowed_source_ranges):
#@   #! Email transfer protocols
#@   #! SMTP: TCP:25
#@   #! POP3: TCP:110
#@   source_check = '!resource.sourceRanges.all(range, range in ' + str(allowed_source_ranges) + ')'
#@   port_checks = [
#@     "resource.allowed.containsFirewallPort('tcp', '25')",
#@     "resource.allowed.containsFirewallPort('tcp', '110')",
#@   ]
#@   return "resource.direction.matches('INGRESS') && " + source_check + " && (" + " || ".join(port_checks) + ")"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/Firewall
condition: #@ build_condition(constraint.params().allowed_source_ranges)
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict VPC Firewall rules allowing mail protocol access from the Internet
description: Ensure that mail protocols (SMTP, POP3) are not accessible from the Internet when using VPC Firewall Rule
#@ end
